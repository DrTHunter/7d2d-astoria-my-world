# Legacy scripts

The one-off scripts that produced the earlier passes over this map (the first 259 mod POIs and the
original 13-build cluster). They are kept for provenance only — they refer to prefab names and a
world layout that no longer exist, and **none of them is needed to install anything**.

To install, use `tools/install.py` one level up.
